# What Read the Docs installs. Keep in sync with the `docs` dependency group in
# pyproject.toml -- RTD does not read uv's groups.
Sphinx>=7.2,<9.0
furo>=2024.1
sphinx-copybutton>=0.5
myst-parser>=2.0
